# Copy to .env or pass with:
#   SMALLOPS_DOCKER_ENV_FILE=scripts/docker/smallops-openrouter.env make test-docker
#
# Do not commit real tokens.
ANTHROPIC_AUTH_TOKEN=sk-or-v1-...
ANTHROPIC_BASE_URL=https://openrouter.ai/api
API_TIMEOUT_MS=3000000

# Claude Code should use the OpenRouter auth token above.
ANTHROPIC_API_KEY=

ANTHROPIC_DEFAULT_OPUS_MODEL=x-ai/grok-4.20
ANTHROPIC_DEFAULT_SONNET_MODEL=anthropic/claude-sonnet-4.6
ANTHROPIC_DEFAULT_HAIKU_MODEL=minimax/minimax-m2.7

# Codex Docker tests use OpenRouter's OpenAI-compatible endpoint:
#   https://openrouter.ai/api/v1
#
# This may be the same key string as ANTHROPIC_AUTH_TOKEN, but it is kept in a
# separate variable so Claude Code and Codex do not share endpoint assumptions.
# Leave unset to derive from OPENROUTER_API_KEY, then ANTHROPIC_AUTH_TOKEN.
# SMALLOPS_CODEX_OPENROUTER_API_KEY=sk-or-v1-...
SMALLOPS_CODEX_MODEL=openai/gpt-5.3-codex
