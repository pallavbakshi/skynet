global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: agp
    static_configs:
      - targets:
          - control-plane:7860
    metrics_path: /observability/metrics
